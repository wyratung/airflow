{
  "XMLPublisher": {
    "host": "localhost",
    "port": 5672,
    "Exchange": "xml.exchange",
    "Queue": "xml.queue",
    "RoutingKey": "xml.key",
    "username": "guest",
    "password": "guest"
  },
  "description": "RabbitMQ configuration for BHYT XML processing pipeline",
  "notes": [
    "Message format: XmlBigSizeMessageQueueDto",
    "Message fields: { Id: Guid, Path: string (S3 path) }",
    "Example Path: s3://my-bucket/path/to/file.xml",
    "Exchange type: direct",
    "Queue is durable",
    "Messages are manually acknowledged after successful processing"
  ]
}
